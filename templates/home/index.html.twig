{% extends 'base.html.twig' %}

{% block title %}Ship My Wine{% endblock %}



{% block body %}
    <div class="container-fluid text-center">
        <h1>Ship My Wine</h1>
        {% for message in app.flashes('success') %}
            <div class="alert alert-danger" role="alert">{{ message }}</div>
        {% endfor %}
        <p>
            texte lorem ici bas
        </p>
    </div>
    <div class="container">
    </div>
    </div>
{% endblock %}

